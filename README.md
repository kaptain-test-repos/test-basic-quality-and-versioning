# Test Quality Versioning

E2E test repo for `basic-quality-and-versioning` workflow in buildon-github-actions.

Tests default git-auto versioning with release branch (`release/1.x`) for version series isolation. Uses `always-strip-one-newline` trailing newline mode and shell token style.
